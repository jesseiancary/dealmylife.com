<% content_for :header do %>
	<h3>User: <%= @user.email %></h3>
<% end %>

<p>Member since <%= @user.created_at.to_s(:long) %></p>

<% if logged_in_user == @user %>
	<div class="actions">
		<%= link_to "edit", edit_user_path(@user), :class => "make-button" %>
	</div>
<% end %>
