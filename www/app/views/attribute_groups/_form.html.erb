<%= form_for(@attribute_group) do |f| %>
	
	<% if @attribute_group.errors.any? %>
		<div id="error_explanation">
			<h2><%= pluralize(@attribute_group.errors.count, "error") %> prohibited this Attribute Group from being saved:</h2>

			<ul>
			<% @attribute_group.errors.full_messages.each do |msg| %>
				<li><%= msg %></li>
			<% end %>
			</ul>
		</div>
	<% end %>
	
	<div class="field">
		<%= f.label :name %><br />
		<%= f.text_field :name %>
	</div>
	
	<div class="actions">
		<%= f.submit %>
	</div>
	
<% end %>