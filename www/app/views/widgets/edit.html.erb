<% content_for :header do %>
	<h3>Edit Widget</h3>
<% end %>

<%= render "form" %>

<div class="actions">
	<%= link_to "Back", widgets_path, :class => "make-button" %>
	<%= link_to "Show", @widget, :class => "make-button" %>
</div>
